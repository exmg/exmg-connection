{
  "name": "exmg-connection",
  "description": "Ex Machina client-server connection",
  "private": true,
  "devDependencies": {
    "babel-plugin-module-resolver": "^2.7.1",
    "babel-preset-env": "^1.5.2",
    "eslint": "^4.0.0",
    "eslint-config-google": "^0.8.0",
    "jest": "^20.0.4"
  },
  "scripts": {
    "lint": "eslint . --ext js --ignore-path .gitignore",
    "test": "jest",
    "watch": "jest --watch"
  }
}
